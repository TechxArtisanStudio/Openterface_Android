<?xml version="1.0" encoding="utf-8"?>
<!-- Landscape touchpad overlay: Mouse guide + scroll strip + L/M/R buttons -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/TouchPad_View"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_gravity="bottom|center_horizontal"
    android:layout_marginBottom="8dp"
    android:layout_marginStart="16dp"
    android:layout_marginEnd="16dp"
    android:visibility="gone"
    tools:context="com.openterface.AOS.activity.MainActivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="8dp"
        android:background="@color/background_light">

        <!-- Mouse Guide Area + Scroll Strip -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal">

            <!-- Mouse Guide (weight=5) -->
            <ScrollView
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="5"
                android:background="@drawable/km_basic_mouse_guide_background"
                android:padding="8dp"
                android:layout_marginEnd="4dp"
                android:maxHeight="120dp">

                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="vertical">

                    <TextView
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:text="Mouse Operations"
                        android:textSize="13sp"
                        android:textStyle="bold"
                        android:textColor="@color/text_primary"
                        android:layout_marginBottom="4dp" />

                    <TextView
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:text="• Single finger: Move cursor\n• Tap: Left click\n• Two finger scroll: Scroll wheel\n• Three finger tap: Middle click\n• Long press: Right click"
                        android:textSize="11sp"
                        android:textColor="@color/text_secondary"
                        android:lineSpacingExtra="2dp" />

                </LinearLayout>

            </ScrollView>

            <!-- Scroll Strip (weight=1) -->
            <com.openterface.AOS.view.BasicPortraitScrollStripView
                android:id="@+id/landscape_scroll_strip"
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:layout_weight="1" />

        </LinearLayout>

        <!-- Mouse Button Strip: L + M + R -->
        <com.openterface.AOS.view.MouseControlStripView
            android:id="@+id/mouseButtonStrip"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:layout_marginTop="6dp" />

    </LinearLayout>
</FrameLayout>
